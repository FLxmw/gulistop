spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: fe043d10-c339-40bd-990d-3f92a612768a
        ext-config:
          - data-id: gulimall-product.yml
            refresh: true
          - data-id: datasource-product.yml
            refresh: true
          - data-id: mybatis.yml
            refresh: true
logging:
  level:
    com.feilong.gulimall: debug